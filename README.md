# Spiking Neural Networks & Oscillations Workshop Game

A simple interactive game to explore the basics of spiking neural networks and oscillations.

## How to Run

1. Create a Python environment using `requirements.txt`:
   ```bash
   pip install -r requirements.txt
   ```
2. Run the game:
   ```bash
   python main.py
   ```
